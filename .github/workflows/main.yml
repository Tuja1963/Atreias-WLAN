name: Atreias-WLAN

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */8 * * *'

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Runa ist auf Empfang
        run: echo "📡 Runa ist auf Empfang und hält Ausschau nach etwas Interessantem aus Atreia."
